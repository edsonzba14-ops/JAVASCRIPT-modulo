const transporte = () => {
  const listaTransporte = ["🚗", "🚙", "🚘", "🚕", "🚓"];

  listaTransporte.forEach((item) => {
    console.log(item === "🚓" || item === "🚗");
  });
};

transporte();
